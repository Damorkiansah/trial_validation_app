# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository layout

This repo currently contains **two applications**:

- **Repo root** (`app/`, `public/`, `config/`, `database/`) — the **legacy production app**: plain PHP, no framework. This is what currently runs in production and must keep working throughout the migration.
- **`new_trial_validation_app/`** — the **new Laravel app** being built to replace it. It has its own `CLAUDE.md` with Laravel-specific commands and architecture notes — read that file when working inside that directory.
- **`MIGRATION_PLAN.md`** (repo root) — the full migration plan: target architecture, SSO bridge design between the two apps, API structure, and phase-by-phase rationale. This CLAUDE.md only summarizes it for quick reference; treat `MIGRATION_PLAN.md` as the source of truth for *why*, and update both files together when decisions change.

## Legacy app (repo root)

### Commands

There is no build step, package manager, or test suite for this app (no `composer.json`, no `phpunit`). It runs directly as plain PHP.

```powershell
# Run locally (from repo root) — uses whatever `php` is on PATH (Laragon's, on this machine)
php -S localhost:8000 -t public
# then open http://localhost:8000
```

Database setup: import `trial_validation_system.sql` first, then apply every file in `database/` in filename (date-prefixed) order — each is a standalone hardening/feature migration, there is no migration runner. See README.md "Upgrade hardening" for context on what each batch changed.

Default logins are listed in `README.md`.

### Architecture

Single front-controller pattern, no framework:

- `public/index.php` — the entire router. Every request comes in here (via `.htaccess` rewrite), gets normalized to a path with `normalize_request_path()`, and matched against a long sequence of `if($path==='...')` / `preg_match` blocks. There is no routing table — to find where a URL is handled, grep `public/index.php` for the literal path string.
- `app/bootstrap.php` — everything else: DB connection (`db()`, raw PDO, no ORM), session/auth helpers (`u()`, `role()`, `require_login()`), the **entire authorization model** (`is_admin()`, `is_staff()`, `is_reviewer()`, `can_edit()`, `can_approve_trial()`, `can_view_trial()`, `scoped_trials_parts()` for row-level visibility), CSRF (`csrf_token()`/`verify_csrf()`), audit logging (`audit_log()`, `logActivity()` → `audit_logs`/`activity_logs` tables), and domain logic like `trial_completeness()`. This file is the closest thing to a "service layer" in this codebase — read it before touching any workflow logic.
- `app/views/*.php` — views, rendered via `view($name, $data)` (wraps `layout.php`) or `partial($name, $data)`. Plain PHP templates, `h()` for HTML-escaping.
- Auth is native PHP session-based (`$_SESSION['user']`), sessions stored as files in `storage/sessions/`. Passwords are bcrypt (`password_verify`) — **compatible with Laravel's default `Hash::make`**, which the migration plan relies on.

Domain model: a "trial" (`trials_header`) moves through a state machine — `Draft → In Review → Ready for Approval → Approved` / `Rejected` / `Need Revision` — with per-department reviews (`trials_review`, keyed by `review_round`), weighing data (`trials_weighing`), parameter results (`trials_results`), and file attachments. Row-level visibility depends heavily on role + department + trial status (see `scoped_trials_parts()` and `can_view_trial()` in `bootstrap.php`) — this is the most complex/risky logic to port during migration.

## Migration status (summary — see `MIGRATION_PLAN.md` for full detail)

Target: Laravel + Inertia.js (React) on a new Ubuntu 26 server, migrated module-by-module (strangler pattern) while the legacy app keeps running, connected to the **same MySQL database**. A separate `/api/v1/*` (Sanctum Bearer token) is deferred until the mobile app project starts, after web migration completes.

**✅ Resolved 2026-08-24:** `new_trial_validation_app/` stays on `laravel/react-starter-kit` (**Inertia.js + React**, Fortify auth) instead of the plain API-only + separate Vite SPA + Sanctum originally described in `MIGRATION_PLAN.md` §2/§5 (draft is now stale on this point). Web pages are `Inertia::render()` responses under `routes/web.php`. `MIGRATION_PLAN.md` §2/§5 has been reconciled accordingly.

**✅ Fase 0 complete (2026-08-24). ✅ Fase 1 complete (2026-08-26). ✅ Fase 2 complete (2026-08-26, incl. print/PDF decision). ✅ Post-Fase-2 UI/UX redesign pass complete (2026-08-26). 🚧 Fase 3 in progress — Trial form (create/edit) done (2026-08-26); Trials sidebar nav + wizard shell done (2026-08-26); Validation (wizard Step 2) done (2026-08-27).** Users, Products, Parameters, Access Rights, Masters, Notifications, Trash, and Activity Logs are all done (Fase 1); Dashboard and the read-only Trials list are done (Fase 2); grouped sidebar nav, modal-based admin forms, toast notifications, and the brand accent are done (redesign pass). See their checklist entries for what's in scope vs deferred. **▶️ Start next session here:** Fase 3's next sub-item, Weighing (Packaging & Filling) — the checklist order (Validation → WeighingPackaging → WeighingFilling → Attachment → Review) was confirmed directly from legacy's real `current_step` writes in `public/index.php` during the prior session. Read `MIGRATION_PLAN.md` §4 for the SSO design (still applies as-is) and §6 Fase 0 for what the RBAC port produced (models, Policy, Gates) before building controllers that need to authorize against trials or admin-only actions. **Also read the 2026-08-24 shared-DB incident note under the Access Rights checklist item below before running any `migrate:fresh`/`migrate:refresh`/`db:wipe` or trusting a bare `--env=` flag in `new_trial_validation_app/`** — the DB was wiped once already this way and has no further backup behind it now.

### Fase 0 — Fondasi
- [x] Scaffold new Laravel app (`new_trial_validation_app/`, Laravel React Starter Kit)
- [x] Reconcile Inertia-based scaffold with MIGRATION_PLAN.md §2/§5 — resolved, keep Inertia (see above)
- [x] Implement `sso_tickets` table + old-app → new-app ticket issue/redirect (design: MIGRATION_PLAN.md §4)
- [x] Implement new-app → old-app ticket consume route (`/sso/consume` in legacy `index.php`)
- [x] End-to-end test of SSO bridge both directions — verified locally 2026-08-24 against the shared MySQL DB (both directions, plus replay/expiry/bogus-ticket rejection). Found and fixed a real bug in the process: Laravel's default app timezone (UTC) didn't match the local MySQL server's timezone (SE Asia Standard Time), so `sso_tickets.expires_at` written by Laravel's `now()` always looked already-expired to legacy's `NOW()`-based checks. Fixed via `new_trial_validation_app/config/app.php` timezone now reading `APP_TIMEZONE` (set to `Asia/Jakarta` locally) — **whichever server ends up hosting the shared MySQL DB (§8) must have its timezone matched by `APP_TIMEZONE`,** or this breaks again.
- [x] Port core RBAC from `bootstrap.php` into Laravel Policies — **done 2026-08-24.** `App\Models\User` got the role/department helpers (`isAdmin()`, `isStaff()`, `isReviewer()`, `reviewDepartmentsForUser()`, etc. — port of `is_admin()`/`is_staff()`/`is_reviewer()`/`reviewer_department_codes()`); new minimal model stubs `Trial`/`TrialReview`/`TrialEditPermission`/`MasterOption` map onto the shared `trials_header`/`trials_review`/`trial_edit_permissions`/`master_options` tables (Fase 3 will flesh these out further); `App\Policies\TrialPolicy` ports `can_view_trial()`/`can_edit()`/`can_approve_trial()`; `Trial::scopeVisibleTo()` ports the row-level list scoping from `scoped_trials_parts()`; simple admin-only checks (`can_manage_master()` etc.) became Gates in `AppServiceProvider`. This also required correcting `App\Models\User` to match the *real* shared `users` schema (`password_hash` not `password`, no `email_verified_at`/`remember_token`/`updated_at` columns) — the fresh-install migration branch (for sqlite/tests) and a few Fortify-adjacent controllers/actions (`SecurityController`, `ProfileController`, `ResetUserPassword`) were adjusted to match. Full test suite passes; spot-verified against real shared MySQL data via tinker. See `MIGRATION_PLAN.md` §6 Fase 0 for more detail.
- [x] Decide physical location of the shared MySQL DB (MIGRATION_PLAN.md §8) — **decided 2026-08-24: stays on the local/shared DB as-is for the whole migration.** The physical move + old→new data cutover happens at go-live (folded into Fase 4, "Turn off legacy PHP app" below), not before. The production-server timezone decision is deferred to that same point — not needed while everything's local.

### Fase 1 — Modul admin/master data (risiko rendah)
- [x] Users — **done 2026-08-24.** Ported the `/admin/users` + `/settings/users` block from legacy `public/index.php` (list+search, create/update-by-email upsert with password reset, soft delete) into `App\Http\Controllers\Admin\UserController` (`admin.users.index/store/destroy` in `routes/admin.php`) + `resources/js/pages/admin/users/index.tsx`. Authorization via new `App\Policies\UserPolicy` (viewAny/create/update/delete — the Super-Admin-target protection rules from legacy live here), following the `TrialPolicy` pattern from Fase 0. `App\Models\User::roleCategories()` ports `role_categories()`. **Deferred to a later pass** (separate legacy screen, Super-Admin-only, higher risk — ties into `trial_edit_permissions`): role-category master CRUD and the whole Access Rights screen (role/department reassignment, reviewer-department master, draft-trial edit-permission grant/revoke) at `/admin/access-rights` in legacy — tracked as the "Access Rights" item below. Found and fixed a real bug while building this: `User`'s `#[Fillable(['name','email','role','department'])]` attribute excludes `password_hash`/`is_active`/`deleted_at`/`deleted_by` by design, so mass-assignment helpers (`updateOrCreate`, `->update([...])`) silently dropped those fields — must set them via direct property assignment (`$user->password_hash = ...; $user->save()`) instead, as the controller now does. Also: `Carbon::now()` (not the `now()` helper) is needed when assigning to a `deleted_at`-typed property, since `AppServiceProvider` rebinds the `Date` facade to `CarbonImmutable` app-wide. Full Pest suite (36 tests incl. 9 new), Pint, Larastan, ESLint, tsc, and `npm run build` all pass.
- [x] Products — **done 2026-08-24.** Ported the `/admin/products` (`/templates/products`) block from legacy `public/index.php` (paginated list, edit-in-place via `?edit={id}`, create-or-update-by-id-or-name upsert, soft delete) into `App\Http\Controllers\Admin\ProductController` (`admin.products.index/store/destroy` in `routes/admin.php`) + `resources/js/pages/admin/products/index.tsx`. No new Policy needed — gated by the `manage-templates` Gate already defined in `AppServiceProvider` from the Fase 0 RBAC port (Admin or Staff role), matching legacy's `can_manage_templates()`. New `App\Models\Product` (`product_name`, `finish_good_code`, `is_active`, `deleted_at`, `deleted_by` — no timestamps, matches the real `products` schema) + a fresh-install migration mirroring the `users` table's pattern (`Schema::hasTable` guard, skipped on the shared MySQL DB). `finish_good_code` confirmed to be plain free-text with no auto-generation logic anywhere in the legacy app before porting. 10 new Pest tests (visibility gating, create, update-by-id, upsert-by-name, name-collision validation error, soft delete); full Pest suite (46 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, and `npm run build` all pass.
- [x] Parameters — **done 2026-08-24.** Ported the `/admin/parameters` (`/templates/parameters`) block from legacy `public/index.php` (paginated list, edit-in-place via `?edit={id}`, create-or-update-by-id save — legacy never upserts by name here, only Products does — soft delete) into `App\Http\Controllers\Admin\ParameterController` (`admin.parameters.index/store/destroy` in `routes/admin.php`) + `resources/js/pages/admin/parameters/index.tsx`, added to the sidebar nav. Reuses the `manage-parameters` Gate already defined in `AppServiceProvider` from the Fase 0 RBAC port (Admin or Staff role), matching legacy's `can_manage_parameters()` exactly — no new Policy needed. New `App\Models\ValidationParameter` (`product_type`, `parameter_name`, `specification`, `sort_order`, `is_active`, `deleted_at`, `deleted_by` — no timestamps, matches the real `validation_parameters` schema) + a fresh-install migration following the `products` pattern. The product-type dropdown reads `master_options` (`type='product_type'`) via the existing `App\Models\MasterOption` stub — this **required adding a fresh-install migration for `master_options` too** (`2026_08_24_000003_create_master_options_table.php`), since no migration for that table existed yet even though `App\Models\User`'s role/department helpers already depended on it (they got away with it via a try/catch fallback to hardcoded defaults, added in the Fase 0 RBAC port — see `App\Models\User::reviewerDepartmentCodes()`/`roleCategories()`); this is schema-only, same shared-DB-skip-creation guard as the other tables, and does **not** front-run the separate Masters CRUD module below. Also added a new `Textarea` shadcn/ui component (`resources/js/components/ui/textarea.tsx`) — didn't exist yet in this scaffold. 9 new Pest tests (visibility gating, create, update-by-id, duplicate-name-always-creates-new since legacy has no name-uniqueness constraint here, validation errors, soft delete); full Pest suite (56 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, and `npm run build` all pass. Spot-checked live against the real shared MySQL DB via `php artisan serve` + a scripted login (admin@local.test) — the page renders (200 OK, correct built JS asset referenced) with real product-type options from the shared DB.
- [x] Access Rights — **done 2026-08-24.** Ported the `/admin/access-rights` block from legacy `public/index.php` (user role/department reassignment, reviewer-department master CRUD, draft-trial edit-permission grant/revoke — all Super-Admin-only, `is_super_admin()` with no Admin fallback) into `App\Http\Controllers\Admin\AccessRightController` (`admin.access-rights.*` in `routes/admin.php`) + `resources/js/pages/admin/access-rights/index.tsx`, linked from the sidebar (Super Admin only). New `manage-access-rights` Gate in `AppServiceProvider`. Added fresh-install migrations for `trials_header` (minimal, matching the existing `Trial` model stub — none existed before this) and `trial_edit_permissions` (none existed before either); added a `unique(type,name)` index to the `master_options` fresh-install migration since no such constraint could be found in any legacy migration file despite legacy's upsert relying on one — the port doesn't depend on the DB constraint anyway (uses an app-level find-then-save, not `ON DUPLICATE KEY UPDATE`). 16 new Pest tests, full suite (72 tests, 3 pre-existing unrelated skips) + Pint + Larastan + ESLint + Prettier + tsc + `npm run build` all pass. Spot-checked live against the real shared MySQL DB (post-recovery, see incident note below) via `php artisan serve` + a scripted login as `admin@local.test` (promoted to Super Admin) — the page renders (200 OK) with real data: 42 users across 3 pages, 6 real Draft trials, 7 Staff users for the grant dropdown.

  **⚠️ Incident + recovery, 2026-08-24:** while sanity-checking the new migrations, an errant `php artisan migrate:fresh --env=testing` was run from `new_trial_validation_app/`. No `.env.testing` file exists in that project, so the flag silently fell back to the default `.env` — which points at this same shared MySQL DB (`trial_validation_system`), not an isolated test DB. `migrate:fresh` dropped every table and recreated only the ones with a Laravel migration, wiping everything else. **Restored the same day**, with the user's explicit approval at each step: the legacy app has no separate DB of its own (`config/database.php` points at the exact same shared DB, confirmed before assuming any recovery path), so the fix was importing `database/trial_validation_system.sql` — a fuller dump generated **2026-07-03** found alongside the smaller, staler root-level copy (generated 2026-05-21) — then reconciling schema against every file in `database/` (their `ADD COLUMN IF NOT EXISTS` syntax doesn't actually run on this MySQL 8.4.3 install, so each file's target columns were checked by hand; only `trials_header.approver_user_id` was genuinely missing and was added directly). `admin@local.test` was promoted from Admin to Super Admin (no Super Admin existed in the July dump) to spot-check the Super-Admin-only Access Rights screen. **Residual loss: the DB is now a 2026-07-03 snapshot, not current** — roughly 7 weeks of activity between then and the wipe is gone for good, and no fresher backup was found. Full detail in memory (`shared_db_wiped_2026_08_24` / `no_env_testing_flag_incident`). Going forward: never trust a bare `--env=` flag on a raw artisan command in `new_trial_validation_app/` without confirming the target `.env.<name>` file actually exists — there is no more spare backup behind this DB now.
- [x] Masters — **done 2026-08-26.** Ported the `/admin/masters` (`/templates/master`) block from legacy `public/index.php` (paginated list ordered by type/sort_order/name, edit-in-place via `?edit={id}`, create-or-update-by-id-or-(type,name) upsert, soft delete) into `App\Http\Controllers\Admin\MasterOptionController` (`admin.masters.index/store/destroy` in `routes/admin.php`) + `resources/js/pages/admin/masters/index.tsx`, added to the sidebar nav. Reuses the `manage-master` Gate already defined in `AppServiceProvider` from the Fase 0 RBAC port (Admin or Staff) — no new Policy needed. Uses the existing `App\Models\MasterOption` stub and its fresh-install migration (both already existed from the Parameters/Access Rights work). Faithfully ports legacy's "privileged type" split: `role_category`/`reviewer_department` are hidden from this screen's list/edit/delete for non-Super-Admins (those two types are managed for real via the separate Access Rights screen instead) — enforced via a dynamic allow-list in `StoreMasterOptionRequest` (5 base types for Admin/Staff, +2 privileged types for Super Admin) plus a 50-char name cap on the privileged types, matching legacy's inline validation. 16 new Pest tests (visibility gating, upsert-by-id, upsert-by-(type,name), privileged-type rejection for non-Super-Admin, privileged-type allow + delete for Super Admin, deleted rows excluded from list); full Pest suite (89 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, and `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) via `php artisan serve` + scripted login as `admin@local.test` (Super Admin) — 200 OK, 122 real master options across 7 pages, correctly ordered by type/sort_order/name.
- [x] Notifications — **done 2026-08-26.** Ported the `/admin/notifications` block from legacy `public/index.php` (paginated list of every notification in the system ordered by created_at/id desc, permanent hard delete — no soft delete, unlike every other Fase 1 module) into `App\Http\Controllers\Admin\NotificationController` (`admin.notifications.index/destroy` in `routes/admin.php`) + `resources/js/pages/admin/notifications/index.tsx`, added to the sidebar nav (Admin-only, unlike the Admin-or-Staff modules above). No new Policy or Gate — reuses the pre-existing `manage-settings` Gate (`isAdmin()`, no Staff fallback) from the Fase 0 RBAC port, which already matched legacy's `is_admin()` check here exactly. New `App\Models\Notification` maps onto the real shared `notifications` table (`user_id`, `role_target`, `department_target`, `trial_id`, `title`, `message`, `type`, `is_read`, `read_at`, `removed_by_user`, `removed_at`, `created_at` — confirmed against the live shared DB's actual columns) with `belongsTo` relations to `User` and `Trial`, eager-loaded for the list's target-user/trial-code columns; its fresh-install migration follows the established `Schema::hasTable` shared-DB-skip pattern. The legacy screen's per-user notification bell/inbox (`/notifications`, with its `notification_user_status` read/removed tracking) is explicitly out of scope — this only ports the admin-only management screen; deleting a notification relies on the shared DB's existing `ON DELETE CASCADE` FK to clean up any `notification_user_status` rows, so the app code doesn't need to touch that table. 6 new Pest tests (non-admin forbidden, admin/super-admin can view, target user + linked trial shown correctly, admin can hard-delete, staff cannot delete); full Pest suite (95 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, and `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) via a scripted authenticated request as `admin@local.test` (Super Admin) dispatched straight through the HTTP kernel — 200 OK, 1573 real notifications across 79 pages, with `user`/`trial` relations resolving correctly against real data.
- [x] Trash — **done 2026-08-26.** Scope decision (see AskUserQuestion resolved this session): legacy's `/admin/trash` (`public/index.php` ~1270-1370) is trial-only (not a generic Products/Parameters/Masters/Users recycle bin despite the Fase 1 grouping) and its permanent-delete cascades DELETEs across `trials_review`/`trials_weighing`/`trials_results`/`trial_attachment_files`/`trial_edit_permissions`/`notifications`/`audit_logs` — tables Fase 3 hasn't ported models for yet. User chose **list + restore now, permanent-delete deferred to Fase 3** rather than raw-SQL-porting the cascade against tables with no Eloquent models. Ported into `App\Http\Controllers\Admin\TrashController` (`admin.trash.index/restore` in `routes/admin.php`) + `resources/js/pages/admin/trash/index.tsx`, added to the sidebar (Admin-only, same as Notifications). Reuses the `manage-settings` Gate (`isAdmin()`) — matches legacy's `is_admin()` check on this screen exactly, no new Policy/Gate needed. Added two relations to the existing `App\Models\Trial` stub: `creator()` (`belongsTo(User::class, 'created_by', 'email')`, matching legacy's `u_creator.email=h.created_by` join) and `deletedByUser()` — deliberately *not* named `deletedBy`, because Eloquent's `toArray()` would then key the loaded relation as `deleted_by`, colliding with and silently overwriting the raw `deleted_by` foreign-key column of the same name in JSON output. Filters (`q` on trial_code/product_name/product_type, `deleted_by` on the deleter's name via `whereHas`, `date_from`/`date_to` on `deleted_at`) port legacy's filter bar exactly. 10 new Pest tests (visibility gating, active-trials excluded, creator/deleter shown correctly, each filter, restore clears `deleted_at`/`deleted_by`, restoring an already-active trial is a no-op, Staff forbidden); full suite (105 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) by dispatching an authenticated request through the HTTP kernel as `admin@local.test` — 200 OK, 35 real trashed trials across 2 pages, first row's `creator`/`deleted_by_user` relations resolving to real user data (Hanbi / Fauzi).
- [x] Activity Logs — **done 2026-08-26.** Ported the `/admin/activity-logs` block from legacy `public/index.php` (paginated, filterable — date range, user, role, module, action, keyword — system-wide audit trail, single delete, bulk delete-selected) into `App\Http\Controllers\Admin\ActivityLogController` (`admin.activity-logs.index/destroy/destroy-selected` in `routes/admin.php`) + `resources/js/pages/admin/activity-logs/index.tsx` (checkbox multi-select with a "Delete Selected" bar, expandable per-row Old/New Data detail panel — same interaction shape as legacy's inline JS toggle), added to the sidebar (Admin-only, same as Notifications/Trash). Reuses the `manage-settings` Gate (`isAdmin()`) — matches legacy's `is_admin()` check on this screen exactly, no new Policy/Gate needed. New `App\Models\ActivityLog` maps onto the real shared `activity_logs` table (columns confirmed against `database/trial_validation_system.sql`); its fresh-install migration follows the established `Schema::hasTable` shared-DB-skip pattern. Like Notifications, this is a genuine **hard delete** (no `deleted_at` on `activity_logs`) — nothing in this app writes to this table yet (legacy's `logActivity()` hasn't been ported; no trial workflow exists here until Fase 3), so the module is read + delete only, same shape as legacy. 8 new Pest tests (visibility gating, module filter, search-keyword filter, single delete, staff forbidden from single delete, bulk delete-selected, staff forbidden from bulk delete); full suite (114 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. **Gotcha hit while building this:** `php artisan wayfinder:generate` (run manually to regenerate TS route/action helpers after adding the controller/routes) defaults to *not* emitting the `.form()` helper every other admin page's delete/bulk-action buttons rely on — running it bare briefly broke every existing admin page's `tsc` check by stripping their already-generated `.form()` methods. Fix: always pass `--with-form` when running this command by hand (`composer run dev` / `npm run dev` / `npm run build` invoke the Vite plugin version of this step automatically and already pass it correctly — this only bites a manual/standalone `artisan wayfinder:generate` call). Live-spot-checked (read-only, no writes against the shared DB) by dispatching an authenticated request through the HTTP `Kernel` as `admin@local.test` (Super Admin) — 200 OK, 2272 real activity log rows resolving via the model, most recent one (`LOGIN`/`AUTH`/`damor@cosmax.com`, 2026-07-03) matching the DB directly.

  This closes out Fase 1 (Modul admin/master data) — Users, Products, Parameters, Access Rights, Masters, Notifications, Trash, and Activity Logs are all done. Fase 2 (Dashboard & Trials list, read-only) is done too — see below. The prior decision to defer visual/styling polish until after Fase 2 still applies.

### Fase 2 — Dashboard & Trials List (read-only)
- [x] Dashboard — **done 2026-08-26.** See `new_trial_validation_app/CLAUDE.md` Fase 2 section for full detail.
- [x] Trials list (read-only) — **done 2026-08-26**, same session as Dashboard above; both share one `TrialsTable` component.
- [x] Decide print/PDF report approach in the new stack — **decided 2026-08-26.** Legacy has no PDF library at all: every report (`report.php`, `report_approved.php`, `report_rejected.php`, `report_trial_summary.php`, `report_department_review.php`, `report_audit_print_log.php`) is a plain view styled with `@media print` CSS and printed via the browser's native `window.print()`, plus a `POST /trials/{id}/print-log` call that writes a `report_printed` audit-log entry before the print dialog opens. For the new stack: **`spatie/browsershot`** (headless Chrome/Puppeteer), generating a real server-side PDF file — chosen over `barryvdh/laravel-dompdf` because the legacy report layouts lean on CSS grid/flexbox (`report-info-grid`, `attachment-grid-print`) that dompdf's CSS engine renders poorly, whereas Browsershot can reuse the same HTML/CSS as the print views almost as-is for close-to-pixel-accurate output. Node.js is already a hard dependency of this project (Vite/npm build), so Browsershot's Node+Chromium requirement isn't new infrastructure, just an additional package on the target Ubuntu 26 server. Not yet implemented — this only records the choice; the actual Fase 3 report controllers/views and the `report_printed` audit-log port still need to be built when Fase 3 reaches the Reports sub-item.

This closes out Fase 2 (Dashboard, Trials list, and the print/PDF decision).

### UI/UX Redesign Pass (post-Fase-2, pre-Fase-3)
- [x] Redesign — **done 2026-08-26.** Grouped sidebar navigation (Overview / Master Data / User Management / System, replacing one flat "Platform" list, collapsing per-role so an empty group never renders); toast notifications activated app-wide (`useFlashToast()` existed since the Fase 0/1 scaffolding but was never called anywhere — one line in `AppLayout` turned it on for every `Inertia::flash('toast', ...)` already being sent by all 8 admin controllers, no backend changes needed); inline-always-visible and `?edit={id}`-reload admin forms (Users, Products, Parameters, Masters, and all three Access Rights sections) converted to `Dialog`-based modal create/edit flows that close automatically on success; new shared `Table`/`PaginationFooter`/`ConfirmDialog` components replacing ~9 pages' worth of duplicated raw-`<table>`/pagination-footer/delete-confirm-dialog markup; Cosmax brand-red accent (`--brand` CSS token, applied to the logo tile, active sidebar item, and focus ring only — Save/Delete buttons stay neutral, since brand red and shadcn's `--destructive` are almost the same hue). Verified with a real headless-browser pass (ad-hoc Playwright via `npx`, not a permanent project dependency — no browser-automation tooling exists in this repo yet) confirming sidebar grouping, modal open/prefill/close-on-success, toast display, and light+dark brand contrast, in addition to the full `composer run ci:check`-equivalent gate (Pint, Larastan, Pest, ESLint, Prettier, tsc, `npm run build`) all passing. See `new_trial_validation_app/CLAUDE.md`'s "UI/UX redesign pass" section for full detail.

This closes out the redesign pass. Fase 3 (Inti workflow trial) started 2026-08-26 with the Trial form sub-item — see below; each remaining sub-item gets its own planning pass when it starts, per `MIGRATION_PLAN.md` §8.

### Fase 3 — Inti workflow trial (paling besar & berisiko, dikerjakan paling akhir)
- [ ] Sub-tahapan lain dirinci saat masing-masing dimulai (belum direncanakan detail — lihat MIGRATION_PLAN.md §8)
- [x] Trial form (create/edit) — **done 2026-08-26.** Ported `/trials/create`, `/trials/store`, `/trials/{id}/edit`, `/trials/{id}/update` from legacy `public/index.php:340-483` (header-only — validation/weighing/review/approval/attachments/reports are separate sub-items below) into `TrialController::create/store/edit/update` (`new_trial_validation_app/routes/trials.php`) + one shared `resources/js/pages/trials/form.tsx` page for both modes. Business logic moved into invokable action classes per `MIGRATION_PLAN.md` §5's intended Fase 3 pattern — `App\Actions\Trials\CreateTrial` (trial_code generation, now with a random-suffix + collision-retry loop fixing a real race condition legacy had with its bare `TRIAL-{Ymd-His}` format; product_name/finish_good_code always re-resolved from the `Product` row, never trusted from client input, matching legacy) and `UpdateTrial` (header-only, never touches progress_status/current_step/trial_code/created_by/revision_no). Validation via `Rule::exists()` against `master_options`/`products` (replacing legacy's manual `option_exists()`/`options_exist()`), shared between `StoreTrialRequest`/`UpdateTrialRequest` through `TrialRequestRules`. `App\Policies\TrialPolicy::create/update` (already ported in Fase 0) gate both — first real usage of that Policy in a controller. Added a `draft` group to `TrialController::GROUPS` (and `Trial::scopeVisibleTo()` already supported it) since newly-created Draft trials had nowhere to be listed otherwise; added `Trial::product()` relation and `array`/`decimal:2` casts for `validation_scope`/`machine_used`/`estimate_qty`. First Fase 3 write to `activity_logs` (`App\Models\ActivityLog`, previously read-only since Fase 1) — CREATE/UPDATE rows now written per trial save; the richer old/new-diff `audit_logs` table stays deferred until the Reports sub-item (`report_audit_print_log`) actually needs it. UI/UX (per the user's explicit ask at Fase 3 kickoff to keep raising the bar, not just hold the redesign-pass baseline): two new shared components, `resources/js/components/combobox.tsx` and `multi-select.tsx` (both Command+Popover-based — new `cmdk`/`@radix-ui/react-popover` deps, new `ui/command.tsx`/`ui/popover.tsx` shadcn primitives), replacing legacy's jQuery Select2 product dropdown and native `<select multiple>` for validation_scope/machine_used (19 and 94 options respectively) with a searchable single-select and a searchable multi-select with removable badge chips; the form itself is Card-sectioned (Product Info / Scope & Machines / Batch & Team / Reason & BOM) rather than one flat field list. 16 new Pest tests (create, every validation rule, Draft-editable-by-owner-or-grant-only — confirmed a super admin with no ownership/grant is correctly *forbidden*, matching legacy's `can_edit()` exactly, unlike `can_view_trial()` which does have a super-admin bypass — Need-Revision editable by any staff, Approved blocked, soft-deleted-trial 404s, draft group listing); full suite (140 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked against the real shared DB: rather than a browser session, dispatched the controller's `create`/`store`/`edit` methods directly in tinker (`Auth::login()` + a manually-validated `StoreTrialRequest`, bypassing HTTP/CSRF plumbing since this was a tinker check, not a request simulation) using real `admin@local.test` + real product/master-option rows — created trial `TRIAL-20260826-152308-YT0T`, confirmed correct Draft/Validation state, correct product snapshot, an `ActivityLog` CREATE row, and visibility in the new `draft` group, then soft-deleted the trial and removed its log row so no fake data lingers in the shared DB.
- [x] Trials sidebar nav + wizard shell — **done 2026-08-26**, same session as the Trial form above, following a user-testing pass on the Edit Trial screen (three items raised via AskUserQuestion: a Combobox/MultiSelect text-overlap bug, no sense of step progress, and "New Trial" only reachable via a Dashboard/Trials-list button). Fixed the overlap bug (`min-w-0 flex-1` missing on the flex `Button` wrapping a `truncate` span in `combobox.tsx`/`multi-select.tsx`, so long product names bled into the next grid column). Added a "Trials" sidebar nav group (`app-sidebar.tsx`) with a role-gated "New Trial" link (`isStaff = role === 'Staff' || isAdmin`, matching `TrialPolicy::create()`, not a bare `role === 'Staff'` check) plus all 6 always-visible status links (Draft/In Review/Ready for Approval/Approved/Need Revision/Rejected) — the group itself is never role-gated since `Trial::scopeVisibleTo()` shows every non-Draft row to every non-super-admin role regardless, an empty departmental-reviewer result is a normal list state, not a reason to hide the nav entry. Added a 6-step wizard shell wrapping the existing trial header form as Step 1 of 6 (`resources/js/lib/trial-wizard.ts` + `resources/js/components/trial-wizard-steps.tsx`, wired into `resources/js/pages/trials/form.tsx`) — steps 2-6 render as visible-but-disabled upcoming entries (no routes exist for them yet), and a `resolveTrialCompletedSteps()` helper reads the trial's real `current_step`/`progress_status` (now added to the frontend `TrialData` type) to show a supplementary note when the shared DB says a trial is further along than the page being viewed (e.g. edited previously via the legacy app). This research also surfaced the checklist-ordering bug fixed above: legacy's actual `current_step` sequence (grepped from `public/index.php`) is Validation → WeighingPackaging → WeighingFilling → Attachment → Review, not Weighing-then-Validation as this file previously listed. No backend changes — frontend-only, no new Pest tests. Live verification against the running app (`composer run dev` + an ad-hoc Playwright script) surfaced two real bugs the automated gate couldn't catch: the Product/Finish Good Code overlap from the earlier checkpoint commit was still reproducible (the earlier `min-w-0` fix was necessary but not sufficient — a CSS Grid min-content-size propagation issue in `form.tsx`'s own field wrappers, not just inside `Combobox`/`MultiSelect`), and the form's outer wrapper was capped at `max-w-4xl` (896px) while every other page in this app is full-width, leaving large unwanted side gaps — both fixed, plus the multi-select's chip list got a tidier bordered container per direct user feedback during the same check. Full CI gate (Pint, Larastan, `php artisan test`, ESLint, Prettier, tsc, `npm run build`) re-run clean after the fixes; full detail in `new_trial_validation_app/CLAUDE.md`'s Fase 3 section.
- [x] Validation — **done 2026-08-27.** Wizard Step 2. Ported `/trials/{id}/validation` (GET) and `/trials/{id}/validation/save` (POST) from legacy `public/index.php:493-559` (view `app/views/validation.php`) into `App\Http\Controllers\TrialValidationController::edit/update` (`trials.validation.edit/update` in `new_trial_validation_app/routes/trials.php`) + `resources/js/pages/trials/validation.tsx`, a new controller rather than folding into `TrialController` (this app's one-controller-per-feature-slice convention — Weighing/Attachment/Review will each need their own edit/update pair). New `App\Models\TrialResult` maps onto the shared `trials_results` table, which has **no surrogate id** — composite primary key `(trial_id, parameter_id)`, confirmed against the real MySQL schema. Adding a surrogate id would need an `ALTER TABLE` against live shared data (this migration is deliberately additive-only, `Schema::hasTable` guarded, same as every other Fase 1/3 migration), so `TrialResult` is Eloquent-read-only by design (`$incrementing = false`, doc comment warning against `save()`/`find()`) — all writes go through `DB::table('trials_results')->upsert()` in the new `App\Actions\Trials\SaveTrialValidation` action, which builds the same `INSERT ... ON DUPLICATE KEY UPDATE` legacy did, batched. That action normalizes each row exactly like legacy (`decision==='N/A'` forces `result='N/A'`; `decision==='OK'` with an empty result forces `'Conform'`), unconditionally advances `trials_header.current_step` to `WeighingPackaging` (`progress_status` untouched — that only changes at a later "submit for review" action, still out of scope), and writes an `ActivityLog` row with `module='VALIDATION'` (a new module value, distinct from `'TRIAL'` — legacy itself logs this under its own `'PARAMETER'` module, confirming even legacy treats it separately from header edits). Save is all-or-nothing: `App\Http\Requests\Trials\SaveTrialValidationRequest` requires every active `validation_parameters` row for the trial's `product_type` to have a submitted decision (`OK`/`NOT OK`/`N/A`), and any `NOT OK` row must have a non-empty Result and Remark — a `withValidator()` cross-field check (precedent: `StoreMasterOptionRequest`) mirrors legacy's PHP loop exactly, attaching every failure to one `results` error key. `TrialController::store()`/`update()` both changed their post-save redirect from `trials.edit` back to itself to `trials.validation.edit` — this is what legacy actually does (confirmed directly in `public/index.php`) and is also what finally makes the wizard shell's "Simpan & Lanjutkan" button true (it just redirected back to the same header form before this). Frontend: one `Card` with a `Table` row per parameter — Decision is a controlled shadcn `Select` (`name` prop lets Radix bubble it into native `FormData` directly, no hidden-input mirror needed) whose `onValueChange` reproduces legacy's inline jQuery auto-fill (`N/A` → forces Result to `N/A`; `OK` on an empty Result → forces `'Conform'`); Result/Remark are plain controlled `Input`/`Textarea` with real `name` attributes. A zero-parameters-configured product_type shows a warning `Alert` and no form (legacy's real limitation, faithfully ported, not improved). 8 new Pest tests (`tests/Feature/TrialValidationTest.php` — non-Draft viewable read-only, no-access 403, soft-deleted 404, mixed OK/NOT-OK/N-A happy path incl. both auto-fill overrides + `current_step` advance + `ActivityLog`, missing-parameter-row rejected, invalid-decision rejected, NOT-OK-missing-remark rejected — all three rejections proven to persist nothing, re-save updates in place proving the upsert path, no-edit-rights staff forbidden); 2 existing `TrialFormTest.php` redirect assertions updated for the new redirect target. Full suite (149 tests, 146 passed/3 pre-existing skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-verified against the real shared DB using trial 80 (`TRIAL-20260826-153659-7RBS`, Tube, the user's own real trial from the previous session's wizard-shell check, deliberately left at `current_step=Validation` for exactly this) — dispatched `TrialValidationController::edit()` (200 OK, real Inertia response) and the `SaveTrialValidation` action directly (matching `CreateTrial`'s prior tinker-based verification pattern) with a realistic mixed payload across its 11 real `Tube` parameters: persisted correctly (`OK`→`Conform` auto-fill, `N/A`→`N/A` auto-fill, the one `NOT OK` row's real result/remark text preserved), `current_step` advanced to `WeighingPackaging`, `progress_status` stayed `Draft`, an `ActivityLog` UPDATE/`VALIDATION` row was written. This is genuine forward progress on the user's real test trial (not synthetic fixture data), so it was deliberately left in place, not cleaned up.
- [ ] Weighing (Packaging & Filling)
- [ ] Review per departemen
- [ ] Approval (e-signature)
- [ ] Reports (approved / rejected / audit print log / department review / trial summary)
- [ ] Attachments/photo upload

### Fase 4 — Decommission
- [ ] Turn off legacy PHP app
- [ ] Remove SSO bridge (`/sso/*` routes both sides, `sso_tickets` table)
- [ ] Finish any deferred DB schema redesign
- [ ] Write API docs for the mobile app project
