# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This is the **new** Laravel app replacing the legacy PHP app at the repo root (`../`). See `../CLAUDE.md` and `../MIGRATION_PLAN.md` for the overall migration plan, phase checklist, and SSO bridge design — this file only covers commands/architecture local to this Laravel codebase.

## Commands

```bash
composer run dev      # php artisan serve + queue:listen + vite dev, all concurrently
composer run lint      # pint --parallel (auto-fix)
composer run lint:check   # pint --parallel --test (check only)
composer run types:check  # phpstan analyse (larastan)
composer run test      # config:clear + lint:check + types:check + php artisan test
composer run ci:check   # npm lint:check + npm format:check + npm types:check + test

php artisan test                                  # full Pest/PHPUnit suite
php artisan test --filter=TestName                # single test by name
php artisan test tests/Feature/DashboardTest.php   # single file

npm run dev          # vite dev server (frontend only, use alongside `php artisan serve` if not using composer run dev)
npm run build         # vite build (add build:ssr for SSR build)
npm run lint          # eslint --fix
npm run format         # prettier --write resources/
npm run types:check      # tsc --noEmit
```

Tests use **Pest** (`tests/Pest.php`), not raw PHPUnit syntax, even though `phpunit.xml` configures the runner.

## Architecture

Official `laravel/react-starter-kit` scaffold — **Inertia.js**, not a decoupled REST API + SPA. Pages are server-rendered React components returned via `Inertia::render()` from controllers in `app/Http/Controllers`; there is no client-side router fetching JSON from a separate API for these pages.

- `resources/js/pages/` — Inertia page components (one per route), grouped by feature (`auth/`, `settings/`).
- `resources/js/layouts/` — shared layout shells (`app/`, `auth/`, `settings/`).
- `resources/js/components/ui/` — shadcn/Radix UI primitives.
- `resources/js/routes/` and `resources/js/actions/` — **generated by Laravel Wayfinder** (`laravel/wayfinder` + `@laravel/vite-plugin-wayfinder`), giving typed TS helpers for Laravel routes/controller actions. Don't hand-edit these — they regenerate from PHP routes/controllers on build.
- `app/Actions/Fortify/` — Laravel Fortify handles auth actions (registration, password reset, etc.); `app/Providers/FortifyServiceProvider.php` wires it up.
- `routes/web.php` / `routes/settings.php` — current routes are all Fortify/starter-kit boilerplate (profile, security settings) — no trial-validation domain routes exist yet.
- `boost.json` / `.mcp.json` — Laravel Boost is configured, providing MCP tools for this Laravel app (schema/route/log introspection etc.) when available in this session.

### RBAC / authorization (ported from legacy `bootstrap.php`, 2026-08-24)

`app/Models/User.php` maps onto the **real** shared `users` table (`password_hash`, `role`, `department`, `is_active` — no `password`/`email_verified_at`/`remember_token`/`updated_at`), not the starter kit's default schema; the fresh-install migration branch (used by sqlite/tests) was changed to match this same shape so behavior is identical in every environment. It carries role/department helper methods (`isAdmin()`, `isStaff()`, `isReviewer()`, `reviewDepartmentsForUser()`, etc.) ported from `is_admin()`/`is_staff()`/`is_reviewer()`/`reviewer_department_codes()` in the legacy app.

`app/Models/Trial.php`, `TrialReview.php`, `TrialEditPermission.php`, `MasterOption.php` are minimal stubs mapping onto the shared `trials_header`/`trials_review`/`trial_edit_permissions`/`master_options` tables — just enough for the RBAC port below; Fase 1 (Masters) and Fase 3 (trial workflow) will flesh them out with real CRUD/relations.

`app/Policies/TrialPolicy.php` ports `can_view_trial()`/`can_edit()`/`can_approve_trial()` (`view`/`update`/`approve` abilities — auto-discovered by Laravel via the `Trial`/`TrialPolicy` naming convention, no manual registration needed). `Trial::scopeVisibleTo()` ports the row-level list-scoping query from `scoped_trials_parts()` — use it wherever a trials list needs to be restricted to what the current user may see. Simple admin-only checks that aren't tied to a model instance (`can_manage_master()` etc.) are registered as Gates in `AppServiceProvider::configureGates()` (`manage-settings`, `manage-master`, `manage-parameters`, `view-products-template`, `manage-templates`).

### Fase 1 progress

**Users** (2026-08-24): `app/Http/Controllers/Admin/UserController.php` (`admin.users.index/store/destroy`, routed from `routes/admin.php`) + `resources/js/pages/admin/users/index.tsx` — list/search, create-or-update-by-email upsert (mirrors legacy's single "Save / Change Password" form), soft delete. `app/Policies/UserPolicy.php` gates it (viewAny/create/update/delete), including the Super-Admin-target protection rules from legacy. `User::roleCategories()` ports `role_categories()`.

Two gotchas hit while building this, relevant to any future model-mutating controller code:
- `User`'s `#[Fillable(['name','email','role','department'])]` attribute deliberately excludes `password_hash`/`is_active`/`deleted_at`/`deleted_by`. Mass-assignment helpers (`updateOrCreate`, `Model::update([...])`) silently drop those fields with no error — set them via direct property assignment (`$user->password_hash = ...; $user->save();`) instead.
- `AppServiceProvider::configureDefaults()` rebinds the `Date` facade to `CarbonImmutable` app-wide, so the `now()` helper returns a `CarbonImmutable`. Assigning that to a property typed as `Carbon` (e.g. `deleted_at`) fails Larastan. Use `\Illuminate\Support\Carbon::now()` explicitly for those assignments.

Legacy's Access Rights screen was deferred at the time this was written — it's since been built, see below.

**Products** (2026-08-24): `app/Http/Controllers/Admin/ProductController.php` (`admin.products.index/store/destroy`, routed from `routes/admin.php`) + `resources/js/pages/admin/products/index.tsx` — paginated list, edit-in-place via `?edit={id}`, create-or-update-by-id-or-name upsert, soft delete. No new Policy — reuses the `manage-templates` Gate from `AppServiceProvider::configureGates()` (Admin or Staff). New `App\Models\Product` maps onto the real shared `products` table (`product_name` unique, `finish_good_code`, `is_active`, `deleted_at`, `deleted_by` — no timestamp columns); its fresh-install migration follows the same `Schema::hasTable` guard pattern as the `users` migration. `finish_good_code` is plain free-text in legacy, no auto-generation logic — confirmed before porting.

**Parameters** (2026-08-24): `app/Http/Controllers/Admin/ParameterController.php` (`admin.parameters.index/store/destroy`, routed from `routes/admin.php`) + `resources/js/pages/admin/parameters/index.tsx`, linked from `app-sidebar.tsx`. No new Policy — reuses the `manage-parameters` Gate (Admin or Staff), same shape as `manage-templates`. New `App\Models\ValidationParameter` maps onto the real shared `validation_parameters` table (`product_type`, `parameter_name`, `specification`, `sort_order`, `is_active`, `deleted_at`, `deleted_by` — no timestamps); its fresh-install migration follows the `products` pattern. Unlike Products, legacy's save here is a plain create-or-update-by-id — there's no upsert-by-name matching, so saving without an `id` always inserts (even on a duplicate `parameter_name`); the port preserves that.

The product-type dropdown on this page reads `master_options` (`type='product_type'`) through the existing `App\Models\MasterOption` stub — this is what finally required adding `2026_08_24_000003_create_master_options_table.php`, a fresh-install migration for `master_options` (schema-only, same shared-DB-skip guard as the other tables). That table had no migration until now even though `User::reviewerDepartmentCodes()`/`roleCategories()` already queried it from the Fase 0 RBAC port — they just never hit a fresh sqlite install without a try/catch fallback masking the gap. This migration does **not** front-run the separate Masters CRUD module (still unbuilt) — it only makes the table exist.

Also added `resources/js/components/ui/textarea.tsx` (shadcn primitive, wasn't in this scaffold yet — needed for the Specification field).

**Access Rights** (2026-08-24): `app/Http/Controllers/Admin/AccessRightController.php` (`admin.access-rights.*` in `routes/admin.php`) + `resources/js/pages/admin/access-rights/index.tsx`, linked from the sidebar (Super Admin only, unlike the Admin-or-Staff modules above). Ports legacy's `/admin/access-rights` block in full: user role/department reassignment (edit-in-place via `?edit={id}`, same pattern as Products/Parameters, since a per-row `<Form>` can't legally nest inside a `<table>`), reviewer-department master CRUD against `master_options` (`type=reviewer_department`), and draft-trial edit-permission grant/revoke against `trial_edit_permissions`. New `manage-access-rights` Gate in `AppServiceProvider` (`isSuperAdmin()`, no Admin fallback — unlike every other Gate in this file). Three Form Requests (`UpdateUserRoleRequest`, `StoreReviewerDepartmentRequest`, `GrantDraftPermissionRequest`).

Two schema gaps surfaced while building this — neither table had ever gotten a fresh-install migration despite `App\Models\Trial`/`TrialEditPermission` existing as Fase-0 stubs, because nothing had exercised them against a fresh DB yet:
- Added `2026_08_24_000004_create_trials_header_table.php` (minimal columns matching `Trial`'s existing `#[Fillable]` list, same shared-DB-skip guard pattern as the others) and `2026_08_24_000005_create_trial_edit_permissions_table.php` (matches `../../database/20260702_trial_edit_permissions.sql` exactly).
- Added `TrialEditPermission::grantedBy()` (`BelongsTo<User>` on `granted_by`) alongside the existing `trial()`/`user()` relations, for the draft-permission list's "granted by" column.
- Added a `unique(type, name)` index to `master_options` in its fresh-install migration (`2026_08_24_000003_...`) — no legacy migration file was found that actually creates this constraint, despite legacy's reviewer-department save relying on `ON DUPLICATE KEY UPDATE` (which needs one to make sense). The port doesn't depend on the DB constraint either way — `storeReviewerDepartment()` does an app-level `firstWhere()`-then-save instead of a raw upsert, so this index is just defensive, not load-bearing.

16 new Pest tests (role reassignment incl. self-edit block and unknown-role rejection, reviewer-department upsert/soft-delete/undelete, grant/revoke incl. owner-already-has-access and non-Draft/non-Staff rejection, re-grant-after-revoke reactivation); full suite, Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked via `php artisan serve` + scripted login as `admin@local.test` (promoted to Super Admin for this, per `../CLAUDE.md`'s 2026-08-24 incident note) — renders correctly against real restored data (42 users, 6 Draft trials, 7 Staff users).

**Masters** (2026-08-26): `app/Http/Controllers/Admin/MasterOptionController.php` (`admin.masters.index/store/destroy`, routed from `routes/admin.php`) + `resources/js/pages/admin/masters/index.tsx`, linked from `app-sidebar.tsx`. No new Policy — reuses the `manage-master` Gate (Admin or Staff), same shape as `manage-templates`/`manage-parameters`. Uses the pre-existing `App\Models\MasterOption` stub and its migration (both already existed, added during the Parameters/Access Rights work) — no schema changes needed this time.

Ports legacy's "privileged type" split faithfully: `role_category` and `reviewer_department` are two `master_options` `type` values that legacy hides from this screen for anyone who isn't Super Admin (excluded from the list query, edit-fetch nulled out, delete blocked) — they're meant to be managed only through the dedicated Access Rights screen (`AccessRightController`). `MasterOptionController::BASE_TYPES` (5 types, what Admin/Staff can use) vs `::PRIVILEGED_TYPES` (2 types, Super-Admin-only) drive both the list-query filter and `StoreMasterOptionRequest`'s dynamic `Rule::in()` allow-list (built from the authenticated user's role); a `withValidator` after-hook adds legacy's 50-char name cap for the privileged types specifically. 16 new Pest tests (visibility gating, upsert-by-id, upsert-by-(type,name) mirroring legacy's `ON DUPLICATE KEY UPDATE`, privileged-type rejected in validation for Admin, privileged-type accepted + deletable for Super Admin, deleted rows excluded from list, privileged rows hidden from non-Super-Admin list but visible to Super Admin); full suite (89 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked (read-only — no writes made against the shared DB) via `php artisan serve` + scripted login as `admin@local.test` (Super Admin) — 200 OK, 122 real master options across 7 paginated pages, correctly ordered by type/sort_order/name; no `role_category`/`reviewer_department` rows exist in the shared DB yet, so the privileged-type-visible-to-Super-Admin path wasn't observable live, only test-covered.

**Notifications** (2026-08-26): `app/Http/Controllers/Admin/NotificationController.php` (`admin.notifications.index/destroy`, routed from `routes/admin.php`) + `resources/js/pages/admin/notifications/index.tsx`, linked from `app-sidebar.tsx` (Admin-only — no Staff, unlike the Products/Parameters/Masters modules above). No new Policy or Gate — reuses `manage-settings` (`isAdmin()` only), which was already sitting unused in `AppServiceProvider` from the Fase 0 RBAC port and turned out to match legacy's `is_admin()` check on `/admin/notifications` exactly. New `App\Models\Notification` maps onto the real shared `notifications` table — columns confirmed directly against the live shared DB via `Schema::getColumnListing()` rather than just the legacy migration files, since two separate legacy SQL files (`20260508_notifications.sql` then `20260508_admin_control_activity_pagination.sql`) each added columns to it. `belongsTo` relations to `User` and `Trial`, eager-loaded (`user:id,name,email`, `trial:id,trial_code`) for the list's target/trial columns. Unlike every other Fase 1 module, this is a genuine **hard delete** — legacy has no `deleted_at` on `notifications`, and the confirm dialog says "permanently" — so `NotificationController::destroy()` just calls `$notification->delete()` with no soft-delete bookkeeping.

Scope note: legacy's `/admin/notifications` delete route also explicitly deletes matching rows from `notification_user_status` before deleting the notification itself; this port skips that and relies on the shared MySQL DB's existing `fk_notification_status_notification ... ON DELETE CASCADE` to do the same cleanup, since `notification_user_status` belongs to the **per-user** notification bell/inbox (`/notifications` in legacy) which is out of scope here and has no migration in this app yet. If that inbox gets ported later, revisit whether this delete path needs its own explicit handling for non-shared-DB (sqlite/test) environments where the cascade FK won't exist unless that table's migration is added too.

6 new Pest tests (non-admin forbidden, admin/super-admin can view, target user + linked trial shown correctly via the eager-loaded relations, admin can hard-delete, staff cannot delete); full suite (95 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) by dispatching an authenticated `Request` straight through the HTTP `Kernel` in tinker as `admin@local.test` (simpler than `php artisan serve` + curl for a read-only check) — 200 OK, 1573 real notifications across 79 pages, first row's `user`/`trial` relations resolving to real data (`Pinto` / `TRIAL-20260618-040920`).

**Trash** (2026-08-26): `app/Http/Controllers/Admin/TrashController.php` (`admin.trash.index/restore`, routed from `routes/admin.php`) + `resources/js/pages/admin/trash/index.tsx`, added to the sidebar (Admin-only, same as Notifications). No new Policy or Gate — reuses `manage-settings` (`isAdmin()`), which already matches legacy's `is_admin()` check on `/admin/trash` exactly. Scope: **list + restore only** — legacy's permanent-delete action there cascades raw `DELETE`s across `trials_review`/`trials_weighing`/`trials_results`/`trial_attachment_files`/`trial_edit_permissions`/`notifications`/`audit_logs`, and several of those tables have no Eloquent model in this app yet (Fase 3 territory); the user explicitly chose to defer permanent-delete rather than raw-SQL-port a cascade against unmodeled tables.

Added two relations to the existing `App\Models\Trial` stub: `creator()` — `belongsTo(User::class, 'created_by', 'email')`, matching legacy's `u_creator.email=h.created_by` join (`created_by` stores an email, not a user id) — and `deletedByUser()`. That second one is deliberately *not* named `deletedBy`: Eloquent's `toArray()`/JSON serialization merges relations and attributes into one array, and a relation named `deletedBy` would serialize to the key `deleted_by`, colliding with (and silently overwriting) the raw `deleted_by` foreign-key column already on the model. Naming it `deletedByUser` keeps both the raw id and the resolved user available in the same payload (`deleted_by` and `deleted_by_user`).

Filters port legacy's filter bar exactly: `q` searches `trial_code`/`product_name`/`product_type` (LIKE), `deleted_by` searches the deleting user's name via `whereHas('deletedByUser', ...)`, `date_from`/`date_to` bound `deleted_at`. 10 new Pest tests (visibility gating, active trials excluded from the list, creator/deleter resolved correctly, each filter individually, restore clears `deleted_at`/`deleted_by`, restoring an already-active trial is a no-op rather than an error, Staff forbidden from both index and restore); full suite (105 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) by dispatching an authenticated request straight through the HTTP `Kernel` as `admin@local.test` (Super Admin) — 200 OK, 35 real trashed trials across 2 pages, first row's `creator`/`deleted_by_user` relations resolving to real data (`Hanbi` / `Fauzi`).

**Activity Logs** (2026-08-26): `app/Http/Controllers/Admin/ActivityLogController.php` (`admin.activity-logs.index/destroy/destroy-selected`, routed from `routes/admin.php`) + `resources/js/pages/admin/activity-logs/index.tsx`, added to `app-sidebar.tsx` (Admin-only, same as Notifications/Trash). No new Policy or Gate — reuses `manage-settings` (`isAdmin()`), matching legacy's `is_admin()` check on `/admin/activity-logs` exactly. New `App\Models\ActivityLog` maps onto the real shared `activity_logs` table (columns confirmed against `../database/trial_validation_system.sql`); its fresh-install migration follows the established `Schema::hasTable` shared-DB-skip pattern used by `notifications`/`master_options`/etc. Filters port legacy's filter bar exactly (`date_from`/`date_to` bound `created_at`, `user` LIKE on `user_name`, exact match on `role`/`module`/`action`, `q` LIKE across `record_label`/`old_data`/`new_data`). Like Notifications, this is a genuine **hard delete** (no `deleted_at` on `activity_logs`) — the UI supports both single delete and legacy's bulk "Delete Selected" (checkbox multi-select + a `destroy-selected` POST route taking `log_ids[]`, since a resource-style `DELETE` route can't carry an array body the same way). Nothing in this app writes to `activity_logs` yet — legacy's `logActivity()` (the function that inserts these rows) hasn't been ported, since no trial workflow exists in this app until Fase 3 — so this module is strictly read + delete, mirroring legacy's own screen (which likewise never writes here, only reads/deletes).

Gotcha hit while building this: `php artisan wayfinder:generate` run bare (no flags) does **not** emit the `.form()` helper that every other admin page's delete/bulk-action `<Form {...XController.destroy.form(id)}>` calls depend on — running it that way to pick up the new controller/routes silently stripped `.form()` from every already-generated action file and broke `tsc` across every existing admin page, not just the new one. Fix: always pass `--with-form` for a manual/standalone `artisan wayfinder:generate` call. `composer run dev`, `npm run dev`, and `npm run build` all invoke the Vite plugin's own generation step instead of the bare artisan command and already pass the right flags — this only bites when running the artisan command directly.

8 new Pest tests (visibility gating for Admin/Super Admin/Staff, module filter, search-keyword filter against `record_label`, single delete, staff forbidden from single delete, bulk delete-selected, staff forbidden from bulk delete); full suite (114 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) by dispatching an authenticated request through the HTTP `Kernel` as `admin@local.test` (Super Admin) — 200 OK, 2272 real activity log rows resolving via the model, most recent row (`LOGIN`/`AUTH`/`damor@cosmax.com`, 2026-07-03) matching a direct DB query.

This closes out Fase 1 — see `../CLAUDE.md`'s Fase 1 checklist for the full list.

### Fase 2 progress

**Dashboard + Trials list** (2026-08-26): `app/Http/Controllers/DashboardController.php` (`dashboard`, routed from `routes/web.php`) + `app/Http/Controllers/TrialController.php` (`trials.index`, routed from the new `routes/trials.php`, `GET /trials/{group}` with `group` constrained via `whereIn` to `approved|in-review|need-revision|rejected|waiting-approval`) — ports the `/dashboard` and 5 status-grouped list pages from legacy `public/index.php` (lines 205-241). Both share one query shape: `Trial::scopeVisibleTo()` (row-level auth, Fase 0) + two new `Trial` methods added this session — `scopeSearch()` (the `q`/`product_type`/`status`/`date_from`/`date_to` filter fields, ported from the filter tail of legacy's `scoped_trials_parts()`) and `summaryCounts()` (the 7 dashboard summary numbers, ported from `trial_summary_counts()` — runs 7 small `COUNT` queries instead of legacy's load-everything-into-PHP-and-bucket approach). Both controllers are read-only by design: legacy's dashboard "New Trial" button and every row's Action column link to pages that don't exist yet (trial create/edit/validation/approval, all Fase 3) — neither is ported here, so this phase only exposes list + search + pagination.

Frontend: `resources/js/lib/trial-status.ts` (new — `TRIAL_STATUSES` list and `trialStatusBadgeClassName()`, a port of legacy's `status_class()`/`status_badge()`) is now the single source for status badge coloring; the pre-existing Trash page's inline copy of the same logic (from the 2026-08-26 Trash work) was refactored to import from here instead of keeping its own copy. `resources/js/components/trials-table.tsx` (new, shared by both pages) renders the 8-column trial table (Trial ID, Product Name, Finish Good Code, Product Type, Status badge, Current Step, Created Date, Pending With — no Action column, per the read-only scope above) using `@tanstack/react-table` (`useReactTable`/`getCoreRowModel`/`flexRender`) plus the same Previous/Next pagination footer pattern every other admin list page already uses. `resources/js/pages/dashboard.tsx` (full rewrite, replacing the starter-kit placeholder) adds the 7 clickable summary cards (Total/Draft link to `dashboard()` with a query string, the other 5 link to their `trials.index` group route) and the 5-field filter bar (search, status select, product-type select, date-from/to). `resources/js/pages/trials/index.tsx` (new) is the same shell minus the summary cards and status select (the group is fixed by the URL segment already).

Two dependency/tooling notes from this session, relevant to any future table-heavy page:
- Installed `@tanstack/react-table` — pinned to `^8`, **not** the `^9` that `npm install @tanstack/react-table` resolves to by default. v9 is a from-scratch rewrite that removes `useReactTable`/`getCoreRowModel` entirely in favor of a different `useTable`/`createCoreRowModel` API; installing it bare breaks the build with `MISSING_EXPORT` errors matching every mainstream tutorial/example (including shadcn's own data-table docs), which all target v8's stable API. Explicitly install `@tanstack/react-table@^8` for any future table work in this app.
- `useReactTable()` trips the React Compiler's `react-hooks/incompatible-library` ESLint rule (a warning, not an error — `npm run lint:check` still exits 0) because the library returns functions that can't be safely memoized. This is expected/known for this library, not a bug to fix; the Compiler just skips memoizing `TrialsTable`, which is fine since it isn't passed into other memoized components.

Filter selects (status, product type) use plain native `<select>` elements, not the shadcn `Select` component — every other Select usage in this codebase so far is inside an uncontrolled `<Form>` with a `defaultValue`, and Radix's `Select` reserves the empty string internally for "no selection," which conflicts with an "All statuses" / "All product types" option needing empty-string semantics for a controlled filter bar. Revisit if a shared controlled-Select filter pattern is needed elsewhere.

7 new Pest tests in `tests/Feature/DashboardTest.php` (summary counts match visible trials, `q` filter, `status` filter, a non-Super-Admin correctly excluded from seeing another user's Draft) plus 6 new tests in `tests/Feature/TrialsListTest.php` (new file — unknown group 404s, `approved` group excludes non-approved, `rejected` group includes both `progress_status=Rejected` and `final_decision=Rejected`, `waiting-approval` maps to `Ready for Approval`, `product_type` filter); full suite (124 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint (1 expected `incompatible-library` warning, see above — 0 errors), Prettier, tsc, and `npm run build` all pass. Live-spot-checked (read-only, no writes against the shared DB) by dispatching authenticated requests through the HTTP `Kernel` as `admin@local.test` (Super Admin) for `/dashboard` and all 5 `/trials/{group}` routes — all 200 OK against real data (43 total trials: 6 Draft, 23 In Review, 3 Ready for Approval, 10 Approved, 1 Need Revision, 0 Rejected — group page counts summed and cross-checked against the dashboard summary).

**Print/PDF report approach** (decided 2026-08-26, same session): `spatie/browsershot` — see `../CLAUDE.md`'s Fase 2 checklist entry for the full rationale (dompdf's weak grid/flexbox support vs. Browsershot reusing the legacy print CSS almost as-is; Node is already a build dependency here). Nothing implemented yet — Fase 3's Reports sub-item still needs the actual controllers/views and the `report_printed` audit-log port.

### UI/UX redesign pass (2026-08-26)

Post-Fase-2, pre-Fase-3 pass requested by the user: group the sidebar menu, make the CRUD screens feel less like overlapping raw forms, and lean more on `Dialog`/toast for interactivity. Superseded a smaller, already-planned styling-only pass (brand token + `Table` primitive + `PaginationFooter`, from a prior session's plan file) by folding it in as a prerequisite rather than doing it separately.

- **Grouped sidebar nav.** `resources/js/components/nav-main.tsx` changed from a single flat `items: NavItem[]` prop under one hardcoded `SidebarGroupLabel>Platform` to a `groups: NavGroup[]` prop (new `NavGroup = { label: string; items: NavItem[] }` type in `resources/js/types/navigation.ts`), rendering one `SidebarGroup` per group and skipping any group with an empty `items` array. `resources/js/components/app-sidebar.tsx` now builds four role-gated groups — **Overview** (Dashboard), **Master Data** (Products/Parameters/Masters, Admin or Staff), **User Management** (Users Admin+, Access Rights Super-Admin-only), **System** (Notifications/Trash/Activity Logs, Admin+) — same role logic as before, just bucketed instead of one flat array. Also deleted `resources/js/components/nav-footer.tsx` (was only used to render the starter-kit's "Repository"/"Documentation" GitHub links, irrelevant to an internal tool) since it had no other callers.
- **Toasts turned on.** `resources/js/hooks/use-flash-toast.ts` + `resources/js/components/ui/sonner.tsx` + the `<Toaster />` mounted in `app.tsx` were all already built (from earlier Fase 1/2 work) but `useFlashToast()` was never called anywhere in the frontend, so every `Inertia::flash('toast', ...)` already being sent by all 8 admin controllers (Users, Products, Parameters, Masters, Notifications, Trash, Activity Logs, Access Rights) was silently discarded. Fix was one line: `useFlashToast()` called at the top of `resources/js/layouts/app-layout.tsx`, which wraps every page except `welcome`/`auth/*`. No backend changes needed.
- **Modal-based create/edit.** Users, Products, Parameters, Masters, and all three Access Rights form blocks (role/department edit, reviewer-department create, draft-permission grant) moved from a permanently-rendered inline `<Form>` (or, for Products/Parameters/Masters/Access-Rights-role-edit, a `?edit={id}` full-page-reload toggle) to a `Dialog` opened by an explicit "Add"/"Edit"/"Grant" button, with local `useState` tracking which row (if any) is being edited instead of a server-provided `editOption`/`editProduct`/`editParameter`/`editUser` prop. Each dialog's `<Form>` gets `onSuccess={() => onOpenChange(false)}` as a **top-level prop**, not nested inside `options` — Inertia's `<Form>` in this version types `options` as `FormComponentOptions` (`Pick<VisitOptions, 'preserveScroll' | ...>`, no callbacks), while `onSuccess`/`onError`/etc. live directly on `FormComponentProps` itself; nesting `onSuccess` inside `options` is a `tsc` error (`TS2353`), caught by `npm run types:check` during this session. Backend controllers/routes were **not changed** — the `?edit={id}` query-param branch in `ProductController`/`ParameterController`/`MasterOptionController`/`AccessRightController::index()` still exists server-side, just unused by the new frontend now (harmless, has its own existing Pest coverage). Users specifically gained an "Edit" row button it never had before (previously create-and-upsert-by-email was the only form, no way to open it pre-filled) — a pure UX addition, no backend change, since `UserController::store()` already upserts by email.
- **Shared components.** New `resources/js/components/ui/table.tsx` (standard shadcn `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell`, didn't exist before — every list page hand-rolled raw `<table>`), `resources/js/components/pagination-footer.tsx` (generalizes the `Page X of Y (Z items)` + Previous/Next block duplicated on every list page), `resources/js/components/confirm-dialog.tsx` (generalizes the ~9 near-identical inline delete/revoke/restore `Dialog` blocks — takes `trigger`/`title`/`description`/`confirmLabel`/`confirmVariant`/`formProps`). `Paginated<T>` moved from ~9 duplicate local definitions (plus one in `trials-table.tsx`) to a single definition in `resources/js/types/ui.ts`, re-exported via `@/types`. All 9 list-rendering surfaces (`trials-table.tsx`, and all 8 admin pages) migrated onto `Table`/`PaginationFooter`; the Activity Logs page's extra checkbox-multiselect + expandable-detail-row pattern carried over unchanged since the `Table` primitives are pure presentational wrappers around the same tags. Activity Logs' bulk "Delete Selected" (which posts an array of ids via `router.post`, not a per-row `<Form>` action, so it doesn't fit `ConfirmDialog`'s `formProps` shape) got its own small inline `Dialog` instead of the browser-native `confirm()` it used before.
- **Brand accent.** New `--brand` CSS var in `resources/css/app.css` (`oklch(0.598 0.232 27.344)` light / `oklch(0.64 0.21 27.34)` dark, Cosmax red, **not** aliased to `--primary` or `--sidebar-accent`), applied to `app-logo.tsx`'s logo tile (`bg-sidebar-primary` → `bg-brand`, icon simplified to always-white since the tile is red in both themes now, not just light), `nav-main.tsx`'s active `SidebarMenuButton` state (`data-[active=true]:bg-brand`), and `--ring`. Save/Delete buttons deliberately untouched — brand red and shadcn's existing `--destructive` are almost the same hue, confirmed during planning. `resources/js/pages/dashboard.tsx`'s 7 summary cards became real `Card` components with a `lucide-react` icon each and a `border-l-4 border-l-brand` accent strip on "Total Trials"; every admin page's filter/create bar also moved from a bare `rounded-lg border p-4` div onto `Card`/`CardContent`, which is most of what actually fixed the "overlapping inputs" complaint on Access Rights (edit-role form, search filter, and table no longer read as three identical unstyled boxes).
- **Verification.** Full `composer run ci:check`-equivalent (Pint, Larastan, `php artisan test` — 124 tests, 121 passed/3 pre-existing skips, unaffected since this was a pure frontend change — plus `npm run lint`, `npm run format`, `npm run types:check`, `npm run build`) all pass. No permanent browser-automation tooling exists in this repo, so this session verified the live UI with an ad-hoc Playwright script (`npx playwright install chromium` + `npm install --no-save playwright`, nothing added to `package.json`/lockfile) driving a real login against the shared DB as `admin@local.test`: confirmed sidebar group labels render exactly as `["Overview","Master Data","User Management","System"]`, the Add/Edit User dialog opens/pre-fills/closes-on-success with a toast ("User berhasil dibuat."), the Masters delete confirmation renders as a `Dialog` (not a native `confirm()`), Access Rights' three sections render as visually distinct `Card` panels, and the brand-red accent is legible in both light and dark mode (toggled via `localStorage.setItem('appearance','dark')`, matching `use-appearance.tsx`'s storage key). One initial round of modal screenshots looked like a ghosted double-exposure; re-captured with a short settle delay after the dialog's 200ms open animation and confirmed it was a screenshot-timing artifact, not a real rendering bug. The one test user created during this check (`UI Test User`) was deleted afterward via `php artisan tinker`.

### Fase 3 progress

**Trial form (create/edit)** (2026-08-26): first Fase 3 sub-item. `app/Http/Controllers/TrialController.php` gained `create`/`store`/`edit`/`update` (`trials.create/store/edit/update` in `routes/trials.php`, placed before the pre-existing `{group}` catch-all — the `{trial}` and `{group}` params both match a single path segment, so `{trial}` needed `->whereNumber('trial')` or an unconstrained GET to `/trials/not-a-real-group` returned 405 instead of the expected 404, since the unconstrained `PUT {trial}` route matched the URI first). Business logic lives in new invokable action classes, `app/Actions/Trials/CreateTrial.php` and `UpdateTrial.php` — this is the pattern `../MIGRATION_PLAN.md` §5 specifically calls for in Fase 3 (`SubmitTrialForReview`, `ApproveTrial`, etc.), first used here. `CreateTrial` generates `trial_code` as `TRIAL-{Ymd-His}-{4 random chars}` with a collision-retry loop, fixing a real race condition in legacy's bare `TRIAL-{Ymd-His}` (no uniqueness check, same-second collision would throw on the DB's unique key); both actions always re-resolve `product_name`/`finish_good_code` from the `Product` row rather than trusting client input, matching legacy exactly. Validation: `app/Http/Requests/Trials/TrialRequestRules.php` (a plain shared static helper, not a FormRequest) is used by both `StoreTrialRequest`/`UpdateTrialRequest`, using `Rule::exists('master_options', 'name')->where('type', ...)->where('is_active', 1)` in place of legacy's manual `option_exists()`/`options_exist()` loops.

`App\Policies\TrialPolicy::create`/`update` (ported back in Fase 0, unused until now) gate both actions — worth noting `update()` has **no** super-admin bypass for a Draft trial (only owner or an active `trial_edit_permissions` grant), unlike `view()` which does allow super admin; this exactly matches legacy's `can_edit()` vs `can_view_trial()` split in `bootstrap.php`, confirmed by a dedicated test (`a super admin without ownership or a grant cannot edit a draft trial`) rather than assumed. `Trial::scopeVisibleTo()` already supported a `'draft'` status group but no route ever used it — added `'draft'` to `TrialController::GROUPS` and the `{group}` route's `whereIn` list so a newly-created Draft trial has somewhere to appear on the trials list; `Trial` also gained a `product()` relation (existed as a column, never as a relation) and `array`/`decimal:2` casts for `validation_scope`/`machine_used`/`estimate_qty` (replacing legacy's hand-rolled JSON encode/decode for the two array columns).

This is also the **first write** to `activity_logs` in this app (`App\Models\ActivityLog` existed since Fase 1 but was read+delete only, per its own doc comment — "nothing writes to it yet") — `CreateTrial`/`UpdateTrial` now insert CREATE/UPDATE rows. The richer old/new-diff `audit_logs` table (legacy's separate `audit_log()` function) has no model here yet and stays deferred — its only real consumer, `report_audit_print_log`, is a later Reports sub-item, so building it now would be speculative.

UI/UX: the user asked at Fase 3 kickoff to keep raising the bar rather than just holding the redesign pass's baseline (see root `CLAUDE.md` memory reference). Two new shared components fill a real gap — nothing in this codebase before this had a searchable select or a multi-select: `resources/js/components/combobox.tsx` (single-select, Command+Popover) replaces legacy's jQuery Select2 product dropdown, and `resources/js/components/multi-select.tsx` (same base, removable `Badge` chips for selected items) replaces legacy's plain `<select multiple>` for `validation_scope`/`machine_used` (19 and 94 real options respectively — a native multi-select doesn't scale well to that). Needed two new npm deps (`cmdk`, `@radix-ui/react-popover`) and two new shadcn primitives (`resources/js/components/ui/command.tsx`, `ui/popover.tsx`). The form itself (`resources/js/pages/trials/form.tsx`, one component for both create and edit) is Card-sectioned (Product Info / Scope & Machines / Batch & Team / Reason & BOM) instead of legacy's one flat field list; a status `Badge` (reusing `trialStatusBadgeClassName`) shows next to the heading in edit mode. Since the Combobox/MultiSelect fields are controlled React state rather than native uncontrolled inputs, they're threaded into Inertia's `<Form>` via hidden `<input>` elements (`name="validation_scope[]"` per selected value, standard PHP/Laravel array-from-repeated-keys parsing) rather than relying on `defaultValue` like the plain `<select>` fields (`product_type`/`validation_category`/`risk_level`, which stayed as ordinary shadcn `Select`s since they don't need search or multi-value). `TrialController::index()`/`DashboardController::index()` both gained a per-row `can_edit` boolean (set via `$trial->setAttribute('can_edit', ...)` so it serializes like a real column despite not being one) and a top-level `canCreateTrial` boolean, driving a "New Trial" button and a Table Action/Edit column that Fase 2 explicitly deferred pending this work.

16 new Pest tests (`tests/Feature/TrialFormTest.php`) — create, every validation rule (missing required field, empty scope/machine array, unknown master-option value, invalid risk level, inactive/soft-deleted product), the Draft-editable-by-owner-or-grant-only / super-admin-forbidden distinction above, Need-Revision editable by any staff, Approved blocked, soft-deleted trial 404s, `draft` group listing. Two real Larastan findings fixed along the way: `Product::findOrFail($id)` type-checks as `Product|Collection<Product>` (the method's signature allows an array `$id`), which PHPStan correctly flagged when accessing `->product_name` on it — switched both actions to `Product::where('id', $id)->firstOrFail()`, whose return type doesn't have that ambiguity. Full suite (140 tests, 3 pre-existing unrelated skips), Pint, Larastan, ESLint, Prettier, tsc, `npm run build` all pass.

Live-spot-checked against the real shared DB, but differently from every prior (read-only) Fase 1/2 spot-check: since this sub-item writes, and no browser-automation tooling is set up as a standing dependency in this repo, verification was done by calling `TrialController::create/store/edit` directly in `php artisan tinker` — `Auth::login($user)` to set the acting user, a `StoreTrialRequest` built manually and run through its real `authorize()`+`validate()` lifecycle via `validateResolved()` (bypassing HTTP/session/CSRF plumbing entirely, since this exercises the exact same PHP as a real request without needing to fake a browser session) — against real `admin@local.test` and real product/master-option rows. Confirmed: correct `Draft`/`Validation` state, correct `product_name`/`finish_good_code` snapshot, an `ActivityLog` CREATE row, visibility via `Trial::scopeVisibleTo($user, 'draft')`, and a 404 (`ModelNotFoundException`) from `edit()` once soft-deleted. The created trial (`TRIAL-20260826-152308-YT0T`) and its `ActivityLog` row were both cleaned up (soft-delete + hard-delete respectively) immediately after, so nothing fake lingers in the shared DB.

**Trials sidebar nav + wizard shell** (2026-08-26/27, same Fase 3 sub-item continued): the user manually exercised the Edit Trial screen after the above and raised three items, all resolved via AskUserQuestion before building: (1) a Combobox/MultiSelect text-overlap bug, (2) no sense of step progress on a ~6-step process, (3) "New Trial" only reachable via a Dashboard/Trials-list button, no nav entry. Scope agreed: build the sidebar entry and a **wizard shell** — Step 1 (the existing header form) fully wired, Steps 2-6 shown as visible-but-disabled placeholders with no real pages behind them yet (each remaining Fase 3 sub-item still gets its own planning pass, starting with Validation next).

- **Sidebar**: new "Trials" group in `app-sidebar.tsx`, second position (after Overview) — a role-gated "New Trial" link (`isStaff = role === 'Staff' || isAdmin`, matching `TrialPolicy::create()` rather than a bare role check) plus 6 always-visible status links reusing the same icons as the dashboard's summary cards. The group itself is never role-gated — `Trial::scopeVisibleTo()` shows every non-Draft trial to every non-super-admin role, so an empty list for a departmental reviewer is a normal state, not a reason to hide the nav entry.
- **Wizard shell**: `resources/js/lib/trial-wizard.ts` (`TRIAL_WIZARD_STEPS`, `resolveTrialCompletedSteps()`) + `resources/js/components/trial-wizard-steps.tsx`, wired into `trials/form.tsx` as `<TrialWizardSteps currentStep={1} trial={trial} />`. `resolveTrialCompletedSteps()` reads the trial's real `current_step`/`progress_status` (added to the frontend `TrialData` type) so a trial already progressed further in the shared DB (e.g. via the legacy app, or a real user's in-progress trial) shows a supplementary note rather than silently understating progress. This research also caught a real checklist-ordering bug in the root `CLAUDE.md`: legacy's actual `current_step` sequence (grepped from `public/index.php`) is `Validation → WeighingPackaging → WeighingFilling → Attachment → Review`, not "Weighing then Validation" as previously listed — corrected there.
- **Clicking "Simpan & Lanjutkan" doesn't advance to Step 2** — this is expected given the shell-only scope: `TrialController::store()`/`update()` both `to_route('trials.edit', $trial)`, i.e. back to the same header form, because no Validation page exists yet to redirect to. The button label ("Simpan & Lanjutkan" / "Save & Continue") is arguably optimistic given that — worth revisiting once the Validation step (next sub-item) actually exists to redirect into.
- **Two real bugs found during live verification, not caught by `tsc`/tests since they're pure runtime/CSS issues**, both root-caused with the app running live (`composer run dev`'s `php artisan serve` on port 8123 + an ad-hoc Playwright script, same no-standing-dependency convention as the redesign pass):
  1. **Product/Finish Good Code text overlap, still present after the "already fixed" `min-w-0` change from the earlier checkpoint commit.** That commit added `min-w-0 flex-1` to the `<span>` *inside* `Combobox`/`MultiSelect`'s trigger button — necessary but not sufficient. Root cause (confirmed via `getComputedStyle` in a live page, not guessed): the `<div className="grid gap-2">` wrapping each field in `form.tsx` is itself a grid item of the parent `grid sm:grid-cols-2` — and a grid item's default "automatic minimum size" is its content's min-content size, which for `white-space: nowrap` text (what `truncate` sets) equals the *entire untruncated string width*. Without `min-w-0` on that wrapping div specifically, Tailwind's `minmax(0,1fr)` column track can't shrink the item, so a long product name pushed the whole column wider than its track and visually bled into the next one. Fixed by adding `min-w-0` to the three field wrappers that hold a `Combobox`/`MultiSelect` (Product, Validation Scope, Machine Used) in `form.tsx` — the component-internal fix from the earlier commit was still correct/needed, just not the whole story.
  2. **The fix above didn't visibly take effect on the first re-check** — turned out `public/hot` didn't exist (no Vite dev server running), so `php artisan serve` was serving the *last production build*, made before these edits. `npm run build` after each frontend edit (not just at the end) is necessary when verifying live against `composer run dev`'s `serve`-only mode; a plain edit-and-reload does nothing without either a running `npm run dev`/Vite HMR or a fresh `npm run build`.
- **Also widened the form and tidied the multi-select while addressing the above**, per direct user feedback on the same live session: `trials/form.tsx`'s outer wrapper changed from `mx-auto max-w-4xl` (896px, everything else in this app is full-width `space-y-6 p-4` — this was the one outlier) to `mx-auto max-w-6xl` (1152px) — noticeably less side gap without stretching two-column fields to unreadable widths on an ultra-wide monitor. `multi-select.tsx`'s selected-chips row gained a `rounded-md border border-dashed p-2` wrapper (previously bare `flex flex-wrap` floating directly under the trigger, looked disorganized once several chips wrapped to multiple lines) and each `Badge`'s label is now wrapped in `<span className="max-w-48 truncate">` as a defensive measure against the same min-content-width issue as bug 1 above, for a single very long option label.
- No backend changes, no new Pest tests (nothing server-side changed). Re-ran the full gate after all fixes: Pint, Larastan, `php artisan test` (140 tests, 137 passed/3 pre-existing skips, unchanged), ESLint (0 errors, 1 pre-existing `incompatible-library` warning), Prettier, `tsc --noEmit` (clean), `npm run build` (clean) all pass. Live-verified post-fix with the same Playwright script: Product/Finish Good Code bounding boxes no longer overlap, the wizard shell renders "Step 1 of 6" with steps 2-6 visibly disabled, and a real non-deleted Draft trial in the shared DB (id 80, `TRIAL-20260826-153659-7RBS` — the user's own real trial from testing this feature, current_step already at `Validation`, left untouched/not cleaned up since it's real user data, not test fixture) round-tripped correctly through the edit page.

**Validation (wizard Step 2)** (2026-08-27): ports legacy's `/trials/{id}/validation` (GET) + `/trials/{id}/validation/save` (POST) (`public/index.php:493-559`, view `app/views/validation.php`) into a new `App\Http\Controllers\TrialValidationController` (`trials.validation.edit/update` in `routes/trials.php`) + `resources/js/pages/trials/validation.tsx` — a separate controller from `TrialController`, matching the one-controller-per-feature-slice shape Weighing/Attachment/Review will each need too.

New `App\Models\TrialResult` (`database/migrations/2026_08_27_000001_create_trials_results_table.php`, same shared-DB-skip-guard pattern as every other Fase 1/3 migration) maps onto `trials_results`, which has **no surrogate `id`** — its real primary key is composite `(trial_id, parameter_id)`. Rather than ALTER the live shared table to add one (a materially bigger, riskier change than anything else in this migration so far), `TrialResult` is deliberately Eloquent-read-only (`$incrementing = false`, doc comment warning against `save()`/`find()`/`create()`) — all writes go through `DB::table('trials_results')->upsert($rows, ['trial_id','parameter_id'], [...])` in the new `App\Actions\Trials\SaveTrialValidation` action, reproducing legacy's `INSERT ... ON DUPLICATE KEY UPDATE`, batched. `Trial::results(): HasMany` was added for symmetry with `product()`/`reviews()`, with the same "read-only, see TrialResult" caveat.

`SaveTrialValidation` normalizes each row exactly like legacy's PHP loop (`decision==='N/A'` forces `result='N/A'`; `decision==='OK'` with an empty result forces `'Conform'`), unconditionally sets `trials_header.current_step='WeighingPackaging'` (never touches `progress_status`), and writes an `ActivityLog` row — `module='VALIDATION'`, a value distinct from `UpdateTrial`'s `'TRIAL'`, matching how legacy itself logs this under its own separate `'PARAMETER'` module rather than folding it into the header-edit log.

Save is all-or-nothing, ported via `App\Http\Requests\Trials\SaveTrialValidationRequest`: declarative rules cover per-row shape (`results.*.decision` via `Rule::in(['OK','NOT OK','N/A'])`), then a `withValidator()` cross-field pass (precedent: `StoreMasterOptionRequest`) loads every active `validation_parameters` row for the trial's `product_type` and checks each one has a submitted row, and that any `NOT OK` row has a non-empty (trimmed) Result and Remark — both checks attach their message to a single `results` error key, matching legacy's `flash('Validation belum lengkap: '.implode(' | ',$errors))` single-message-list UX (Inertia's default error-bag behavior surfaces only the *first* added message per key, so the frontend renders `errors.results` as one string, not a list — confirmed by reading `inertiajs/inertia-laravel`'s `resolveValidationErrors()`, which only returns `$errors[0]` per key unless `withAllErrors` is explicitly enabled, which this app doesn't do).

`TrialController::store()`/`update()` both had their post-save redirect changed from `trials.edit` (back to itself) to `trials.validation.edit` — this is what legacy's `/trials/store` and `/trials/{id}/update` actually do (confirmed directly in `public/index.php`), and it's also what makes the wizard shell's "Simpan & Lanjutkan" button (added in the previous session, but until now redirected right back to the header form since Validation didn't exist) finally true. Two existing `TrialFormTest.php` assertions were updated for the new redirect target.

Frontend (`resources/js/pages/trials/validation.tsx`): one `Card` containing a `Table` (the shared `ui/table.tsx` primitives) with one row per active parameter for the trial's `product_type`, each row a small local component holding `useState` for decision/result/remark. Decision is a controlled shadcn `Select` — passing `name={\`results[${i}][decision]\`}` directly to `Select` works because Radix's `SelectPrimitive.Root` renders a native bubble input when given a `name`, so it participates in the surrounding `<Form>`'s native `FormData` submission without needing a hidden-input mirror (confirmed by reading `@radix-ui/react-select`'s source, not assumed) — `onValueChange` reproduces legacy's inline jQuery auto-fill (`N/A` forces Result to `N/A`; `OK` on an empty Result forces `'Conform'`). Result/Remark are plain controlled `Input`/`Textarea` with real `name` attributes (no Radix involved, so no bubble-input concern). A zero-parameters-configured `product_type` renders a warning `Alert` and no form at all — legacy's real limitation (a trial can get stuck at Step 2 until an Admin/Staff configures parameters for that type), faithfully ported rather than improved. The non-editable-viewer Back-link falls back to the trials list for the trial's status group (a small local `trialListGroupFor()` mapping, since legacy's own non-editable fallback is a `/report` page this app hasn't built yet).

8 new Pest tests (`tests/Feature/TrialValidationTest.php` — non-Draft trial viewable read-only with `canEdit:false`, no-access 403, soft-deleted 404, mixed OK/NOT-OK/N-A happy path proving both auto-fill overrides plus `current_step` advance plus the `ActivityLog` row, missing-parameter-row / invalid-decision / NOT-OK-missing-remark all rejected with nothing persisted, re-save updates existing rows in place proving the upsert path rather than a duplicate-key error, no-edit-rights staff forbidden on PUT). One Larastan finding fixed along the way, same root cause as `UpdateTrial`'s earlier one: `Trial::findOrFail()` (called via `whereNull('deleted_at')->findOrFail(...)`) type-checks as `Trial|Collection<Trial>` since the underlying method's signature allows an array `$id` — switched to `Trial::whereNull('deleted_at')->where('id', ...)->firstOrFail()` in both places `SaveTrialValidationRequest` needed the trial (its `authorize()` and its `withValidator()` closure); also had to explicitly cast `(array) $this->input('results', [])` before `collect()` since PHPStan can't resolve `collect()`'s template types from a bare `mixed`. Full suite (149 tests, 146 passed/3 pre-existing skips), Pint, Larastan, ESLint (0 errors, same 1 pre-existing `incompatible-library` warning as before), Prettier, tsc, `npm run build` all pass.

Live-verified against the real shared DB using trial 80 (`TRIAL-20260826-153659-7RBS`, `Tube`, the prior session's own real test trial — deliberately left at `current_step=Validation` for exactly this session to pick up, not a synthetic fixture): dispatched `TrialValidationController::edit()` directly (200 OK, real `Inertia\Response`) and the `SaveTrialValidation` action directly (matching `CreateTrial`'s prior tinker-based verification approach) with a realistic mixed payload across the trial's 11 real `Tube` parameters — 10 `OK` (correctly auto-filled to `Conform`), 1 `N/A` (correctly auto-filled to `N/A`), 1 `NOT OK` with real Indonesian result/remark text preserved verbatim. Confirmed `current_step` advanced to `WeighingPackaging`, `progress_status` stayed `Draft`, and a real `ActivityLog` UPDATE/`VALIDATION` row was written. This is genuine forward progress on the user's own real trial, not throwaway test data, so — unlike `CreateTrial`'s verification, which cleaned up its synthetic trial afterward — nothing here was reverted.

### Not yet built

Weighing, per-department review, approval (e-signature), reports, and attachments are all still legacy-only — each is its own remaining Fase 3 checklist item (see root `CLAUDE.md`), to be planned individually as Fase 1 was.

### Reconciling with MIGRATION_PLAN.md

`../MIGRATION_PLAN.md` §5 was drafted assuming a pure JSON API (Sanctum token/cookie auth + API Resources) for *every* screen, to keep the web and future mobile app on the same contract. This scaffold being Inertia-based changes that for the web side:

- Web pages → `Inertia::render()` from normal controllers, authenticated via standard Laravel session (Fortify) — this is actually simpler than the original plan's cookie-based Sanctum SPA setup, and the SSO bridge design (ticket → `Auth::login()`) still applies unchanged.
- Mobile app (Fase 4+) will still need a separate `/api/v1/*` namespace with Sanctum **token** auth and API Resources, since Inertia responses aren't consumable by a mobile client.

This hasn't been confirmed with the user yet — flagged in `../CLAUDE.md`.
